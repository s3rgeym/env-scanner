services:
  php:
    image: php:8.2-fpm-alpine
    volumes:
      - ./testdata/www:/var/www/html

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./testdata/www:/var/www/html
      - ./testdata/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
